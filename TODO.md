# TODO: Fix UserController agar Semua Role Bisa Digunakan

## Steps
- [x] Fix `app/Models/User.php` — tambahkan `role` dan `branch_id` ke Fillable, tambahkan helper methods untuk semua role
- [x] Fix `resources/views/users/edit.blade.php` — tambahkan input password dan konfirmasi password
- [x] Verifikasi hasil

